allowed_users = [178405895]
default_opencode_dir = "/Users/nick/tools/programmable-assistant"

[opencode.defaults]
plan = "smart"
build = "fast"

[opencode.aliases.fast]
provider = "opencode-go"
model = "deepseek-v4-pro"
thinking = "high"

[opencode.aliases.smart]
provider = "opencode-go"
model = "glm-5.1"
thinking = "high"

[opencode.aliases.direct-deepseek]
provider = "deepseek"
model = "deepseek-v4-pro"
thinking = "high"

[opencode.aliases.direct-deepseek-flash]
provider = "deepseek"
model = "deepseek-v4-flash"
thinking = "low"
